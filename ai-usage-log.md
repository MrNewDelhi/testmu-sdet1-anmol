# AI Usage Log

## 2026-07-03

| AI Tool | Used For | Output Produced |
| --- | --- | --- |
| ChatGpt 5.5 Light using Codex | Repository setup and assignment scaffold planning | Created the initial folder structure, copied the assignment PDF, prepared `README.md`, prepared `TODO.md`, added starter project configuration, and prepared the first commit. |
| ChatGpt 5.5 Light using Codex | Task 2 prompt engineering and generated test-case preparation | Created raw module prompts, generated Login/Dashboard/API test-case specs, added module prompt-iteration notes, and updated README/TODO for Task 2. |
| ChatGpt 5.5 Light using Codex | Task 3 v1 naive self-healing implementation | Added xAI selector-repair client, DOM snapshot capture, naive self-healing locator, demo page/test, GitHub Actions workflow, environment examples, documentation, and visualizer. |
| ChatGpt 5.5 Light using Codex | Task 3 v2 centralized self-healing framework | Added a framework-level self-healing service, Playwright fixture, centralized demo test, docs, workflow update, and visualizer updates. |
| ChatGpt 5.5 Light using Codex | Self-healing visualizer workflow improvement | Converted the visualizer from static explanation into animated Play/Step/Reset workflow playback for v1 and v2. |
| ChatGpt 5.5 Light using Codex | Generated-case cleanup and self-healing scenario analysis | Converted Task 2 generated cases from skipped Playwright tests into generated artifacts, added a summary command, excluded generated artifacts from default test collection, and documented self-healing usefulness/refusal scenarios. |
| ChatGpt 5.5 Light using Codex | Generated case automation with POM | Added page objects, fixtures, user factory, and split executable specs for 21 Login/Dashboard/API generated cases, then ran the full generated suite green. |
| Claude Code (Opus 4.8) | Task 2 coverage review and fixes | Diagnosed that the Dashboard module was missing the assignment's data-accuracy and filter/sort scenarios and that several assertions passed without testing (brute-force regex, rate-limit `[200,429]`, navigation-order filler). Used Playwright MCP to explore the live app for real selectors and the lockout message, then added `CatalogPage`/`EditAccountPage` POM classes, catalog data-accuracy + filter/sort tests, hardened the brute-force lockout and API CRUD/404 cases, removed POM leaks, and brought the suite to 26 executable cases (all green). |
| Claude Code (Opus 4.8) | Self-healing visualizer rewrite | Rewrote `public/self-healing-visualizer.html` as a theme-aware, data-driven page whose steps mirror the real `XaiClient`/`SelfHealingService` code, with a request/response packet animation and an honest roadmap tab; verified rendering via Playwright. |
| Claude Code (Opus 4.8) | Task 3 v3: confidence gate + SQLite locator cache | Added programmatic confidence scoring (uniqueness + selector stability, capped by the model's self-reported value) with a refusal threshold, and a dependency-free SQLite locator cache (`node:sqlite`) keyed on (page, broken selector, intent). Wired both into `SelfHealingService`, added unit tests and an end-to-end test proving one xAI call then a cache hit, and added a sample output. All tests green. |
| Claude Code (Opus 4.8) | Task 3 v4: contract disambiguation | Diagnosed that the real risk is a confident-but-wrong heal (a header nav link vs the form submit) sailing through existence + confidence checks, and that the cause is weak output verification, not weak input context. Added a structured target contract (`role`/`type`/`withinForm`/`textLike`) validated in the browser, wired refusal-on-violation/ambiguity into `SelfHealingService`, added a two-control demo page and a demo (decoy refused, target accepted, live xAI steered), added a v4 visualizer tab, and updated docs/sample output. All self-healing tests green. |
| Claude Code (Opus 4.8) | Task 3 v5: deterministic-first (token cost) | Added a SelectorHub-style deterministic resolver that narrows interactive elements by contract or unique intent keywords and rebuilds a stable, verified locator locally, so xAI is only called when resolution is genuinely ambiguous. Wired it ahead of the LLM in `SelfHealingService`, added a demo proving zero xAI calls on resolvable cases plus an escalation case, added a v5 visualizer tab, and isolated the v2/v3/v4 demos (no-op resolver) so each tests its own layer. |
| Claude Code (Opus 4.8) | Task 3 v6: multi-locator resilient cache | Extended the cache to store each target's top-3 attribute-diverse locators (id / data-testid / name / aria-label / form+type) rather than one selector, and made cache reads try them in rank order so single-attribute drift heals from a fallback with no LLM call (tagged v6-multi-locator). Added a demo that renames the primary id and heals via data-testid, a v6 visualizer tab, and docs/sample output. All 6 versions now have a visualizer tab and a demo script; full self-healing suite green. |
| Claude Code (Opus 4.8) | Task 3 v7: Failure Explainer (Option A) | Critiqued the "after each test" trigger (rejected it: analyze only on the final failed attempt, or retries make you pay to explain flakes that pass), then built the assignment's core deliverable — a real xAI `explainFailure` call plus an auto-fixture that captures page state + error on the final failure, attaches a categorized plain-English explanation (product-bug / environment / flaky / test-bug) to the report, with a per-run budget, dedup, and no-key skip. Added a custom FailureAnalysisReporter (failure-analysis.html/json), a deliberately-failing demo behind its own config/script, an always-green in-suite integration test with a real LLM call, a v7 visualizer tab, and docs/sample output. Verified end to end against live xAI. |
