# Copy to .env for local runs. Never commit real secret values.

# xAI is used for the naive v1 self-healing locator.
XAI_API_KEY=
XAI_MODEL=grok-4.3

# Local demo page used by the v1 self-healing test.
BASE_URL=http://127.0.0.1:9323
